<!DOCTYPE html>
<html lang="es">
  <head>
  	<meta charset="utf-8">
    <title>Quiz</title>

    <link rel='stylesheet' type="text/css" 
      href='/stylesheets/style.css' />
    <link rel='stylesheet' type="text/css"
      media='only screen and (min-width:530px) and (min-device-width: 481px)'
      href='/stylesheets/wide.css' />
    <link rel='stylesheet' type="text/css"
      media='only screen and (max-width:481px)'
      href='/stylesheets/smartphone.css' />

    <link rel='icon' href='favicon.ico' type="image/x-icon" />
  </head>
  <body>
    <div id="page-wrap">

      <header class="main" id="h1">
        <h2 style="text-align: center;">Quiz<span>: el juego de las preguntas</span></h2>

        <!-- Control de sesión -->
        <% if(!session.user){ %>
          <span class="right">
            <a href="/login">Login</a>
          </span>
        <% }else{ %>
          <span class="right">
            Bienvenido <%= session.user.username %><p>
            <a href="/logout">Logout</a>
          </span>
        <% }%>
      </header>

      <nav class="main" id="n1" role="navigation">
        <span><a href="/">Inicio</a></span>
        <span><a href="/quizes">Preguntas</a></span>
        <span><a href="/quizes/search">Búsqueda</a></span>
        <span><a href="/quizes/author">Autor</a></span>
      </nav>

      <section class="main" id="s1"> 
        <% if (errors.length) { %>
          <span id='ErrorMsgs'>
            Corregir errores:
            <% for (var iIndex in errors) { %>
              <br/><span> <%= errors[iIndex].message %> </span>
            <% } %>
          </span>
        <% } %>
        <div><%- body %></div>
      </section>

      <footer class="main" id="f1">
        <p>
          <a href="http://es.wikipedia.org/wiki/Quiz">
            ¿Qué es un Quiz?
          </a>
        </p>
        <p>
          <a href="https://github.com/aaronplanell/quiz">
            GitHub
          </a>
        </p>
      </footer>
    </div>
  </body>
</html>
